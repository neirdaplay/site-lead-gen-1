{
  "name": "mon-couvreur-nord",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page de generation de leads couverture — Nord (59). Site statique sans build.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
